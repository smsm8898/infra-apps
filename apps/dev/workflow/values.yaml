environment: dev

# DO NOT EDIT THIS SECTION
argocd:
  project: default
  namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

applications:
  namespace: airflow
  airflow:
    enabled: true
    version: "1.18.0"
